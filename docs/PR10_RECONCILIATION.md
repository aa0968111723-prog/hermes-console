# PR #10 相容新版的決策紀錄

## 保留並接通

- 延伸主持稿、互動卡、IG 限動與自由修改：讀取目前保存的 Workflow，而非固定三套校園模板。
- 每次延伸建立同專案新對話草稿，保留原對話、原方向與原成果；使用者按送出後才沿用持久化 Hermes 任務執行。
- Markdown 匯出透過受保護、owner-scoped API，讀取後端保存的方向與來源。沒有自動補造日期、免費活動或 Canva 網址。
- 主分支的 Runtime、記憶、憑證設定、FrameLab、Lumen 與現有製作/发布確認控制全部保留。

## 不重新引入

原 PR 新增的 local-brain、第二套本地記憶/工具/會話/编排器、固定研究文案、假 Canva export_preview、sandbox 發布成功與不檢查閘道的平行 API 已移除。舊測試中要求这些模板/假成果的案例與已退休實作一起移除，不以排除測試或放寬型別掩蓋不相容；新增真實 handler 的方向匯出/隔離/草稿契約測試，並執行主分支完整測試。

刪除僅影響本 PR 分支的追蹤程式，不刪除工作區資料。舊實作及進度文件可在提交 9539f4d 查閱；Git 歷史未重寫。歷史進度文件不是目前能力的驗證報告。

尚缺正式外部端點及授權，測試不得寫成 Zeabur/Canva 帳號實測。此 PR 不部署、不發佈社群，也不自動合併。

## 驗證（2026-09-07）

- 主分支測試加方向匯出/隔離/延伸回歸，共 126 項通過；typecheck、production build 通過。
- 秘密掃描檢查 241 個來源/建置檔案通過，不代表部署憑證已撤銷。
- 真實 Chrome 與 production Console：390px/1440px 方向顯示、後端 Markdown 下載、延伸至同專案新對話、原方向選擇不變、不自動啟動任務，全部通過。同時驗證既有活動核對、文案版本、學習樹持久化。
- 截圖：忽略提交的 output/playwright/directions-desktop.png 與 directions-mobile-390.png；方向由明確標示 TEST ONLY 的隔離測試資料建立，未宣稱 Hermes 生成。
